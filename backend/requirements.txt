# ============================================================
# KnowledgeGraph v4.0 后端依赖
# ============================================================

# Web 框架
fastapi==0.135.2
uvicorn==0.42.0
python-multipart==0.0.22

# 数据库
sqlalchemy==2.0.51
aiosqlite==0.22.1

# AI / LLM
openai==2.30.0
httpx==0.28.1

# Embedding / Reranker
sentence-transformers>=2.7.0,<6  # v4.1: 范围钉版 — numpy/torch 跨平台版本矩阵由解析器决定
torch==2.12.1

# 检索
lancedb==0.24.3
pyarrow==22.0.0

# 图计算 (Phase 1: GraphRAG 基础)
networkx==3.6.1
python-louvain>=0.16,<1

# 数据处理
numpy>=1.26,<3  # v4.1: 范围钉版 — Windows/Linux 下与 paddle/lancedb 的兼容版本不同
pandas==2.3.3  # v4.1: LanceDB to_pandas() 必需（此前靠本地 venv 隐式存在，CI 缺失）

# NLP
jieba==0.42.1

# 文档解析
pypdf==6.13.3
python-docx==1.2.0
python-pptx==1.0.2
ebooklib==0.20
filetype==1.2.0

# v3.2: Q3 多模态图像处理
paddlepaddle>=3.0.0,<4  # PaddlePaddle CPU (OCR)
paddleocr>=2.9.0,<3  # PaddleOCR 中文识别
Pillow==12.1.1
transformers>=4.40.0,<6

# v3.2: Q8 联网搜索
duckduckgo-search==5.0  # DuckDuckGo 免费搜索

# 缓存
cachetools==7.1.7

# 工具
pydantic==2.12.5
python-dotenv==1.2.2

# 测试
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-httpx==0.36.2
