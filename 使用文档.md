# 知识图谱系统使用文档

## 1. 系统简介

知识图谱系统是一个基于 GraphRAG 技术的智能知识管理平台，使用智谱AI进行实体和关系提取，Neo4j作为图数据库存储。系统提供智能问答、图谱可视化、文档管理和系统设置等功能。

## 2. 环境搭建

### 2.1 硬件要求
- 至少 4GB 内存
- 50GB 存储空间
- 网络连接

### 2.2 软件要求
- Node.js 16+ （前端）
- Python 3.8+ （后端）
- Neo4j 4.0+ （图数据库）

### 2.3 安装步骤

1. **安装 Node.js**
   - 访问 [Node.js 官网](https://nodejs.org/) 下载并安装
   - 验证安装：`node -v`

2. **安装 Python**
   - 访问 [Python 官网](https://www.python.org/) 下载并安装
   - 验证安装：`python --version`

3. **安装 Neo4j**
   - 访问 [Neo4j 官网](https://neo4j.com/) 下载并安装
   - 启动 Neo4j 服务
   - 访问 http://localhost:7474 设置初始密码

4. **安装项目依赖**
   - 前端依赖：`cd frontend && npm install`
   - 后端依赖：`cd ragtest && pip install -r requirements.txt`

## 3. 智谱AI API 获取教程

### 3.1 注册智谱AI账号

1. 访问 [智谱AI开放平台](https://open.bigmodel.cn/)
2. 点击 "注册" 按钮，创建账号
3. 完成邮箱验证和个人信息填写

### 3.2 创建API密钥

1. 登录智谱AI开放平台
2. 进入 "控制台"
3. 点击 "API密钥管理"
4. 点击 "创建密钥"
5. 复制生成的API密钥

### 3.3 API密钥配置

#### 方式一：通过 .env 文件配置（推荐）

1. 在 `ragtest` 目录下创建 `.env` 文件
2. 填写以下内容：

```env
# 智谱AI API配置
API_KEY=your-api-key  # 替换为您的API密钥
API_BASE_URL=https://open.bigmodel.cn/api/messages
MODEL=glm-4

# Neo4j数据库配置
NEO4J_URI=bolt://localhost:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=your-password  # 替换为您的Neo4j密码
```

#### 方式二：通过系统设置页面配置

1. 启动系统后，访问 `http://localhost:5173/settings`
2. 在 "API配置" 标签页中填写 API 密钥和相关参数
3. 点击 "保存配置" 按钮

## 4. 系统启动

### 4.1 使用启动脚本

**Windows 用户**：
- 双击 `start.bat` 文件
- 脚本会自动检查环境、创建默认配置文件、启动前后端服务

**Linux/Mac 用户**：
- 执行 `chmod +x start.sh && ./start.sh`
- 脚本会自动检查环境、创建默认配置文件、启动前后端服务

### 4.2 手动启动

1. **启动前端服务**
   ```bash
   cd frontend
   npm run dev
   ```

2. **启动后端服务**
   ```bash
   cd ragtest
   python -m uvicorn main:app --reload
   ```

3. **访问系统**
   - 打开浏览器访问：http://localhost:5173

## 5. 系统使用指南

### 5.1 文档管理

1. **上传文档**
   - 点击左侧导航栏 "文档管理"
   - 点击 "上传文档" 按钮
   - 选择要上传的文档（支持 PDF、Word、TXT、Markdown 格式）

2. **处理文档**
   - 对于状态为 "未处理" 的文档，点击 "处理" 按钮
   - 系统会使用智谱AI提取文档中的实体和关系

3. **查看文档详情**
   - 点击 "查看" 按钮查看文档详情
   - 可查看文档的处理统计信息

### 5.2 图谱可视化

1. **查看知识图谱**
   - 点击左侧导航栏 "图谱可视化"
   - 系统会显示生成的知识图谱

2. **搜索节点**
   - 在搜索框中输入节点名称
   - 点击搜索按钮

3. **调整布局**
   - 选择不同的布局选项（力导向、环形、树形等）

4. **导出图谱**
   - 点击 "导出" 按钮，选择导出格式

### 5.3 智能问答

1. **提问**
   - 点击左侧导航栏 "智能问答"
   - 在输入框中输入问题
   - 点击 "发送" 按钮

2. **查看历史**
   - 系统会保存对话历史
   - 可查看之前的问答记录

3. **复制答案**
   - 点击答案下方的 "复制" 按钮

4. **反馈**
   - 对答案进行 thumbs up/down 反馈

### 5.4 系统设置

1. **API配置**
   - 点击左侧导航栏 "系统设置"
   - 在 "API配置" 标签页中设置智谱AI API参数

2. **系统参数**
   - 在 "系统参数" 标签页中调整批处理大小、文本块大小等参数

3. **视觉设置**
   - 在 "视觉设置" 标签页中调整主题、主色调等

4. **数据管理**
   - 在 "数据管理" 标签页中查看数据统计、清除缓存等

## 6. API接口说明

### 6.1 后端API

#### 6.1.1 文档处理
- `POST /api/documents/upload` - 上传文档
- `GET /api/documents` - 获取文档列表
- `POST /api/documents/{id}/process` - 处理文档
- `GET /api/documents/{id}` - 获取文档详情
- `DELETE /api/documents/{id}` - 删除文档

#### 6.1.2 知识图谱
- `GET /api/graph/nodes` - 获取节点列表
- `GET /api/graph/relationships` - 获取关系列表
- `GET /api/graph/search` - 搜索节点

#### 6.1.3 智能问答
- `POST /api/chat` - 发送问题并获取回答
- `GET /api/chat/history` - 获取对话历史

### 6.2 前端API

前端通过 axios 与后端API进行交互，所有API调用都封装在相应的组件中。

## 7. 常见问题

### 7.1 API密钥问题
- **问题**：API密钥无效
  **解决方案**：检查API密钥是否正确，是否过期

- **问题**：API调用失败
  **解决方案**：检查网络连接，确认API密钥权限

### 7.2 数据库问题
- **问题**：Neo4j连接失败
  **解决方案**：检查Neo4j服务是否运行，配置是否正确

- **问题**：数据库连接超时
  **解决方案**：增加连接超时时间，检查网络稳定性

### 7.3 文档处理问题
- **问题**：文档处理失败
  **解决方案**：检查文档格式是否支持，文件大小是否超过限制

- **问题**：处理速度慢
  **解决方案**：调整批处理大小和文本块大小

## 8. 故障排除

### 8.1 前端问题
- 清除浏览器缓存
- 检查控制台错误信息
- 确认前端服务是否运行

### 8.2 后端问题
- 检查后端服务日志
- 确认Python依赖是否安装正确
- 检查环境变量配置

### 8.3 数据库问题
- 检查Neo4j服务状态
- 确认数据库连接参数
- 查看Neo4j日志

## 9. 性能优化

### 9.1 前端优化
- 使用浏览器缓存
- 优化图片和资源
- 减少API调用次数

### 9.2 后端优化
- 使用缓存机制
- 优化数据库查询
- 并行处理文档

### 9.3 数据库优化
- 创建适当的索引
- 定期清理无用数据
- 优化Neo4j配置

## 10. 安全注意事项

- **API密钥保护**：不要将API密钥硬编码在代码中，使用环境变量或配置文件
- **数据安全**：敏感数据加密存储
- **访问控制**：实现适当的权限控制
- **输入验证**：对所有用户输入进行验证

## 11. 联系方式

如有问题或建议，请通过以下方式联系：

- GitHub Issues
- 邮箱：your-email@example.com

---

**版本**：1.0.0
**最后更新**：2026-03-30